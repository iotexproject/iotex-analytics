type Query {
    exchanges(height: String!, pagination: Pagination!): [Exchange]!
    tipHeight: String!
}

type Exchange {
    address: String!
    token: String!
    liquidity: String!
    balanceOfToken: String!
    balanceOfIOTX: String!
}

input Pagination{
    skip: Int!
    first: Int!
}